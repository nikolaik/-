{
  "name": "man-with-turban-mega-keyboard",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "start": "reveal-md slides.md -w --css custom.css",
    "build": "reveal-md slides.md --css custom.css --static _site"
  },
  "devDependencies": {
    "reveal-md": "^2.0.2"
  }
}
